import { FunctionComponent } from "react";

const TableTitle: FunctionComponent = ({ children }) => {
  return <p className="font-title text-green-25 text-md">{children}</p>;
};

export default TableTitle;
